= bootstrap_form_tag url: search_path, method: :post, class: 'mb-3' do |f|
  = f.search_field :query, value: @query, label: t('activerecord.attributes.mail_user.name_or_mail'), required: true
  = f.submit t('actions.search')

p = t('descriptions.search_form')
