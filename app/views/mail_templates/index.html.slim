h1 Listing mail_templates

table
  thead
    tr
      th Name
      th Recipient list
      th From name
      th From mail address
      th Subject pre
      th Subject post
      th Body header
      th Body footer
      th Count
      th Reservation time
      th Description
      th
      th
      th

  tbody
    - @mail_templates.each do |mail_template|
      tr
        td = mail_template.name
        td = mail_template.recipient_list
        td = mail_template.from_name
        td = mail_template.from_mail_address
        td = mail_template.subject_pre
        td = mail_template.subject_post
        td = mail_template.body_header
        td = mail_template.body_footer
        td = mail_template.count
        td = mail_template.reservation_time
        td = mail_template.description
        td = link_to 'Show', mail_template
        td = link_to 'Edit', edit_mail_template_path(mail_template)
        td = link_to 'Destroy', mail_template, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Mail template', new_mail_template_path
